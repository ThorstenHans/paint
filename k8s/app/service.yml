apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: spas-in-azure
  labels:
    app: paint
spec:
  selector:
    app: paint
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 80
  type: LoadBalancer
